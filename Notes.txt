2026-05-28 

Task 1: Build 2 APIs for public Search of freelancer and Jobs With a querry. [Done]
Task 2: Set up redux and RTK Query for an API Calls to backend. You can take reference from your servigo project on how to do it. after doing it build 1 search result page and test the search API if it working or not [Done]
Task 3: Get the Search results for both freelancers and job from the backend display them well in your frontend make the UI Impressive [Done]
Task 4: Build find-work and find-talent page where in work page we will return all of the jobs the latest being the first to be returned and in find-talent page we will return top 10 freelancers from each of the 12 categories so for the job the API is already built we do not have to do anything but for talent we need to build an API to get top 8 talent from each categories 

2026-05-29 

Task 1: Develop and Test the API to return top 8 freelancers of each category.[Done]
Task 2: Create an RTK Query to get these top freelancers and display them in a good UI in find talent page in our frontend 
Task 3: Test the API to return all the tops and after that call that API through RTK Query and the display the results in the FindWork page in a Very good UI [Done]
Task 4: Build the API to get all the Jobs by Certain Category and then call the API create that page [Done]
Task 5: Build 1 Choose Page where the user will chose whether they want to create a profile as freelancer or client when we click on signup page we should move to this page [Done] 
Task 7: There is 1 Bug We should be able to signup with our e-mail not phone number but an email because nodemailer is free [Done]
Task 8: Create an RTK Query to call the login API then Build the login Page and complete the login functionanlity for the freelancer. 

2026-05-30 

Task 1: Make Your 3 Step Profile Onboarding of the freelancer with good UI and responsive [Done]

Task 2: Create one userSlice and then update the state in that after  the user has been onboarded and their sills added and later if we change any updates of the user then also we will update this state. We can take reference of the project servigo for this. [Done]

Task 3: So user has created their profile and entered into our application so what will they see they will se a freelance Navbar at the top then the main section which holds the jobs matching their profile in left section in right they will se some content and again when they click on their profile they will see a hamburger menu icon so the first task is to create FreelanceNavbar Component.. 

Task 4: Test the API that returns the jobs based on the freelancer profile and create RTK Query for that and create a beautiful jobs section in main page of the site. 


